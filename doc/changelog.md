# Changelog

## 0.1.0

- Added native Windows `Listen to this device` control through endpoint properties
- Added typed input and output models with device state reporting
- Added native listen configuration read and write support
- Added output-device assignment support, including the default playback device mode
- Added the `audio_monitor_property_dump` diagnostic tool
- Updated the example app, README, and docs for the Windows-native workflow

## 0.0.1

- Initial Flutter desktop plugin structure for `audio_monitor`
